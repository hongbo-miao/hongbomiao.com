[tool.poetry]
name = "hm-airflow"
version = "1.0.0"
description = ""
authors = ["Hongbo Miao"]

[tool.poetry.dependencies]
python = "3.12.x"
apache-airflow = {version = "2.8.0", extras = ["celery", "s3"]}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
