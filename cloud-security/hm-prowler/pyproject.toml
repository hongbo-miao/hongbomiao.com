[project]
name = "hm-prowler"
version = "1.0.0"
requires-python = "~=3.12.0"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
    "poethepoet==0.30.0",
    "prowler==4.5.3",
    "pytest==8.3.3",
    "pytest-cov==6.0.0",
]

[tool.poe.tasks]
test = "pytest --verbose --verbose"
test-coverage = "pytest --cov=. --cov-report=xml"

prowler = "prowler"
