{
  "extends": "solhint:recommended",
  "plugins": ["prettier"],
  "rules": {
    "avoid-sha3": "warn",
    "avoid-suicide": "error",
    "compiler-version": ["error", "^0.8.0"],
    "prettier/prettier": "error"
  }
}
