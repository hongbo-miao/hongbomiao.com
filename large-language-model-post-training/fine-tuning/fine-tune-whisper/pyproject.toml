[project]
name = "fine-tune-whisper"
version = "1.0.0"
dependencies = [
  "datasets==4.4.2",
  "evaluate==0.4.6",
  "jiwer==4.0.0",
  "torch==2.9.1",
  "transformers[audio,torch]==4.57.3",
]

[dependency-groups]
dev = [
  "poethepoet==0.38.0",
  "pytest-cov==7.0.0",
  "pytest==9.0.2",
]

[tool.uv]
package = false
required-version = ">=0.9.0"

[tool.poe.tasks]
dev = "torchrun --nproc_per_node=1 src/main.py"
test = "pytest --verbose --verbose"
test-coverage = "pytest --cov=. --cov-report=xml"
