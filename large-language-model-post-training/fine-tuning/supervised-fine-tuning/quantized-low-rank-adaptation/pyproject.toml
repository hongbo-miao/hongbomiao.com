[project]
name = "supervised-fine-tuning-quantized-low-rank-adaptation"
version = "1.0.0"
requires-python = "==3.14.2"
dependencies = [
  "bitsandbytes==0.49.1",
  "datasets==4.4.2",
  "peft==0.18.1",
  "transformers==4.57.3",
  "trl==0.26.2",
]

[dependency-groups]
dev = [
  "poethepoet==0.40.0",
  "pytest-cov==7.0.0",
  "pytest==9.0.2",
]

[tool.uv]
package = false
required-version = ">=0.9.0"

[tool.poe.tasks]
dev = "accelerate launch src/main.py"
test = "pytest --verbose --verbose"
test-coverage = "pytest --cov=. --cov-report=xml"
