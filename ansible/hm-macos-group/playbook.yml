---
- name: Install packages
  hosts: hm-macos
  tasks:
    - name: Install packages from Homebrew
      community.general.homebrew:
        name:
          - clang-format
          - hadolint
          - jq
          - poetry
          - rclone
        state: latest
