---
- name: Install common packages
  hosts: hm-macos
  tasks:
    - name: Install common packages from Homebrew
      community.general.homebrew:
        name:
          - clang-format
          - hadolint
          - jq
          - poetry
          - rclone
        state: latest
