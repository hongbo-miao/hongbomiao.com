apiVersion: v1
kind: Service
metadata:
  name: {{ include "hm-chart.fullname" . }}
  labels:
    {{- include "hm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hm-chart.selectorLabels" . | nindent 4 }}
