# https://skypilot.readthedocs.io/en/latest/reference/yaml-spec.html

---
name: hm-mlflow-classify-fashion-mnist
resources:
  cloud: aws
  accelerators: T4:1
workdir: .
envs:
  WANDB_API_KEY: xxx
setup: |
  curl -sSL https://install.python-poetry.org | python3 -
  poetry install --no-root
run: |
  poetry run poe dev
