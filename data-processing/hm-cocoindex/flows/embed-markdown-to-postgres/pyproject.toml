[project]
name = "cocoindex-embed-markdown-to-postgres"
version = "1.0.0"
requires-python = "==3.14.2"
dependencies = [
  "cocoindex[embeddings]==0.3.26",
  "pydantic==2.12.5",
]

[dependency-groups]
dev = [
  "poethepoet==0.40.0",
  "prefect==3.6.10",
  "pytest-cov==7.0.0",
  "pytest==9.0.2",
]

[tool.uv]
package = false
required-version = ">=0.9.0"

[tool.poe.tasks]
dev = "cocoindex update src/main.py"
test = "pytest --verbose --verbose"
test-coverage = "pytest --cov=. --cov-report=xml"
