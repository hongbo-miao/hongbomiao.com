[tool.poetry]
name = "hm-api-python"
version = "1.0.0"
description = ""
authors = ["Hongbo Miao"]

[tool.poetry.dependencies]
python = "3.10.x"
Flask = "2.2.2"
Flask-APScheduler = "1.12.4"
Flask-Cors = "3.0.10"
flask-sock = "0.5.2"
python-dotenv = "0.21.0"

[tool.poetry.group.dev.dependencies]
gunicorn = "20.1.0"
poethepoet = "0.16.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poe.tasks]
dev = "flask run"
serve = "gunicorn 'hm_api_python:create_app()' --bind=:35903 --workers=2"
