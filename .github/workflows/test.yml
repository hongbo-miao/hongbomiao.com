---
name: Test

"on":
  pull_request:
    branches: [main]
  schedule:
    - cron: 0 12 * * 0-6

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  codeql:
    name: CodeQL
    uses: ./.github/workflows/codeql.yml
