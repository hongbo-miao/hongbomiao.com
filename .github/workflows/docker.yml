name: Docker

on:
  push:
    branches:
      - main

jobs:
  push-web:
    name: Push web to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push Docker image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./web/Dockerfile
          push: true
          tags: hongbomiao/hm-web:latest
      - name: Print Docker image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-api-node:
    name: Push api-node to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push Docker image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./api-node/Dockerfile
          push: true
          tags: hongbomiao/hm-api-node:latest
      - name: Print Docker image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-graphql-server:
    name: Push graphql-server to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push Docker image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./api-go/build/package/graphql_server/Dockerfile
          push: true
          tags: hongbomiao/hm-graphql-server:latest
      - name: Print Docker image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-grpc-server:
    name: Push grpc-server to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push Docker image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./api-go/build/package/grpc_server/Dockerfile
          push: true
          tags: hongbomiao/hm-grpc-server:latest
      - name: Print Docker image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-config-server:
    name: Push config-server to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push Docker image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./api-go/build/package/config_server/Dockerfile
          push: true
          tags: hongbomiao/hm-config-server:latest
      - name: Print Docker image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-decision-logger:
    name: Push decision-logger to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push Docker image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./api-go/build/package/decision_logger/Dockerfile
          push: true
          tags: hongbomiao/hm-decision-logger:latest
      - name: Print Docker image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-hm-opal-client:
    name: Push opal-client to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push Docker image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./hm-opal-client/Dockerfile
          push: true
          tags: hongbomiao/hm-opal-client:latest
      - name: Print Docker image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-kafka-connect:
    name: Push kafka-connect to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push Docker image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./kafka-connect/Dockerfile
          push: true
          tags: hongbomiao/hm-kafka-connect:latest
      - name: Print Docker image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-chatbot:
    name: Push chatbot to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push Docker image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./chatbot/Dockerfile
          push: true
          tags: hongbomiao/hm-chatbot:latest
      - name: Print Docker image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-hm-alpine:
    name: Push hm-alpine to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push Docker image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./hm-alpine/Dockerfile
          push: true
          tags: hongbomiao/hm-alpine:latest
      - name: Print Docker image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
