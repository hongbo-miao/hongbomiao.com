name: Docker

on:
  push:
    branches: [ main ]

jobs:
  push-web:
    name: Push web to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./web/Dockerfile
          push: true
          tags: hongbomiao/hm-web:latest
      - name: Print image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-api-node:
    name: Push api-node to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./api-node/Dockerfile
          push: true
          tags: hongbomiao/hm-api-node:latest
      - name: Print image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-graphql-server:
    name: Push graphql-server to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./api-go/build/package/graphql_server/Dockerfile
          push: true
          tags: hongbomiao/hm-graphql-server:latest
      - name: Print image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-grpc-server:
    name: Push grpc-server to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./api-go/build/package/grpc_server/Dockerfile
          push: true
          tags: hongbomiao/hm-grpc-server:latest
      - name: Print image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-log-receiver:
    name: Push log-receiver to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./api-go/build/package/log_receiver/Dockerfile
          push: true
          tags: hongbomiao/hm-log-receiver:latest
      - name: Print image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-opa-data-server:
    name: Push opa-data-server to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./api-go/build/package/opa_data_server/Dockerfile
          push: true
          tags: hongbomiao/hm-opa-data-server:latest
      - name: Print image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  push-opal-client:
    name: Push opal-client to Docker Hub
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_ID }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: Build and push image
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./opal-client/Dockerfile
          push: true
          tags: hongbomiao/hm-opal-client:latest
      - name: Print image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
