[tool.poetry]
name = "hugging-face"
version = "1.0.0"
description = ""
authors = ["Hongbo Miao"]

[tool.poetry.dependencies]
python = "3.10.x"
torch = "2.0.0"
transformers = {version = "4.27.3", extras = ["sentencepiece"]}

[tool.poetry.group.dev.dependencies]
poethepoet = "0.19.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poe.tasks]
sentiment-analysis = "python sentiment-analysis.py"
