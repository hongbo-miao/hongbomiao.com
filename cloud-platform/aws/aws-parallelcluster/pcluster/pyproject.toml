[project]
name = "pcluster"
version = "1.0.0"
requires-python = "==3.14.6"

[dependency-groups]
dev = [
  "aws-parallelcluster==3.15.1",
  "poethepoet==0.48.0",
  "pytest-cov==7.1.0",
  "pytest==9.1.1",
]

[tool.uv]
package = false
required-version = ">=0.11.0"

[tool.poe.tasks]
test = "pytest --verbose --verbose"
test-coverage = "pytest --cov=. --cov-report=xml"
