[project]
name = "prefect-daft-analysis"
version = "1.0.0"
dependencies = [
  "daft[aws,deltalake,ray]==0.6.14",
  "prefect-client==3.6.5",
]

[dependency-groups]
dev = [
  "poethepoet==0.38.0",
  "prefect==3.6.5",
  "pytest-cov==7.0.0",
  "pytest==9.0.1",
]

[tool.poe.tasks]
dev = "python src/main.py"
test = "pytest --verbose --verbose"
test-coverage = "pytest --cov=. --cov-report=xml"
deploy = "python src/deploy.py"
run = "prefect deployment run"
