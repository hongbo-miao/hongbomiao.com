[project]
name = "prefect-daft-analysis"
version = "1.0.0"
dependencies = [
  "daft[aws,deltalake,ray]==0.6.4",
  "prefect-client==3.4.20",
]

[dependency-groups]
dev = [
  "poethepoet==0.37.0",
  "prefect==3.4.21",
  "pytest-cov==7.0.0",
  "pytest==8.4.2",
]

[tool.poe.tasks]
dev = "python src/main.py"
test = "pytest --verbose --verbose"
test-coverage = "pytest --cov=. --cov-report=xml"
deploy = "python src/deploy.py"
run = "prefect deployment run"
