[project]
name = "hm-pulumi"
version = "1.0.0"
dependencies = [
  "pulumi==3.206.0",
  "pulumi-aws==7.11.1",
]

[dependency-groups]
dev = [
  "poethepoet==0.37.0",
  "pytest-cov==7.0.0",
  "pytest==9.0.0",
]

[tool.uv]
package = false
required-version = ">=0.8.0"

[tool.poe.tasks]
test = "pytest --verbose --verbose"
test-coverage = "pytest --cov=. --cov-report=xml"
