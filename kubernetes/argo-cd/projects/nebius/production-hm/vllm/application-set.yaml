---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vllm
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/nebius/production-hm/vllm/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-vllm'
    spec:
      project: production-hm
      sources:
        - repoURL: https://vllm-project.github.io/production-stack
          targetRevision: '{{.helmChartVersion}}'
          chart: vllm-stack
          helm:
            releaseName: vllm
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-vllm'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
