# https://doc.traefik.io/traefik/routing/providers/kubernetes-ingress
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hm-vllm-ingress
  namespace: production-hm-vllm
  annotations:
    kubernetes.io/ingress.class: traefik
    # https://doc.traefik.io/traefik/routing/providers/kubernetes-ingress/#on-ingress
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    # https://kubernetes-sigs.github.io/external-dns/latest/docs/annotations/annotations
    external-dns.alpha.kubernetes.io/hostname: vllm.hongbomiao.com
  labels:
    app.kubernetes.io/name: hm-vllm-ingress
    app.kubernetes.io/part-of: production-hm-vllm
spec:
  rules:
    - host: vllm.hongbomiao.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vllm-router-service
                port:
                  number: 80
  tls:
    - hosts:
        - vllm.hongbomiao.com
      secretName: production-hm-vllm-certificate
