---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-prometheus-nats-exporter
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/nebius/production-hm/prometheus-nats-exporter/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-prometheus-nats-exporter'
    spec:
      project: production-hm
      sources:
        - repoURL: https://prometheus-community.github.io/helm-charts
          targetRevision: '{{.helmChartVersion}}'
          chart: prometheus-nats-exporter
          helm:
            releaseName: prometheus-nats-exporter
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
      destination:
        namespace: '{{.environment}}-hm-prometheus-nats-exporter'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
