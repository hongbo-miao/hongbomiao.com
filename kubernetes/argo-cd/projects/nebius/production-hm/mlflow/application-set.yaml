---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: mlflow
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/nebius/production-hm/mlflow/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-mlflow'
    spec:
      project: production-hm
      sources:
        - repoURL: https://community-charts.github.io/helm-charts
          targetRevision: '{{.helmChartVersion}}'
          chart: mlflow
          helm:
            releaseName: mlflow
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-mlflow'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
