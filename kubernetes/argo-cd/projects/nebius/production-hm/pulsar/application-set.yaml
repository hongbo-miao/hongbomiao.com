---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-pulsar
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/nebius/production-hm/pulsar/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-pulsar'
    spec:
      project: production-hm
      sources:
        - repoURL: https://pulsar.apache.org/charts
          targetRevision: '{{.helmChartVersion}}'
          chart: pulsar
          helm:
            releaseName: pulsar
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-pulsar'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
