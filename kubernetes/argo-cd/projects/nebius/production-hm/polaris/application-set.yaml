---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-polaris
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/nebius/production-hm/polaris/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-polaris'
    spec:
      project: production-hm
      sources:
        - repoURL: https://downloads.apache.org/incubator/polaris/helm-chart
          targetRevision: '{{.helmChartVersion}}'
          chart: polaris
          helm:
            releaseName: polaris
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-polaris'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
