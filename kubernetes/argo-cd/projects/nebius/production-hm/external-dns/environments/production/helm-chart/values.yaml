# https://github.com/kubernetes-sigs/external-dns/blob/master/charts/external-dns/values.yaml
---
logLevel: info
image:
  repository: registry.k8s.io/external-dns/external-dns
sources:
  - service
  - ingress
  - traefik-proxy
extraArgs:
  # https://kubernetes-sigs.github.io/external-dns/v0.14.1/tutorials/traefik-proxy/#disabling-resource-listeners
  - --traefik-disable-legacy
provider:
  name: cloudflare
env:
  - name: CF_API_TOKEN
    valueFrom:
      secretKeyRef:
        name: hm-external-dns-secret
        key: CLOUDFLARE_API_TOKEN
domainFilters:
  - hongbomiao.com
policy: upsert-only
registry: txt
txtOwnerId: hm-external-dns
resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 100m
    memory: 256Mi
