---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-prometheus-postgres-exporter
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/nebius/production-hm/prometheus-postgres-exporter/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-prometheus-postgres-exporter'
    spec:
      project: production-hm
      sources:
        - repoURL: https://prometheus-community.github.io/helm-charts
          targetRevision: '{{.helmChartVersion}}'
          chart: prometheus-postgres-exporter
          helm:
            releaseName: prometheus-postgres-exporter
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-prometheus-postgres-exporter'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
