---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-apicurio-registry
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/nebius/production-hm/apicurio-registry/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-apicurio-registry'
    spec:
      project: production-hm
      sources:
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-apicurio-registry'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
