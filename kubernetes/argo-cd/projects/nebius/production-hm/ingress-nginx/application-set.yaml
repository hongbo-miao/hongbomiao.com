---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-ingress-nginx
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/nebius/production-hm/ingress-nginx/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-ingress-nginx'
    spec:
      project: production-hm
      sources:
        - repoURL: https://kubernetes.github.io/ingress-nginx
          targetRevision: '{{.helmChartVersion}}'
          chart: ingress-nginx
          helm:
            releaseName: ingress-nginx
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
      destination:
        namespace: '{{.environment}}-hm-ingress-nginx'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
