---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-prometheus-prefect-exporter
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/nebius/production-hm/prometheus-prefect-exporter/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-prometheus-prefect-exporter'
    spec:
      project: production-hm
      sources:
        - repoURL: https://prefecthq.github.io/prefect-helm
          targetRevision: '{{.helmChartVersion}}'
          chart: prometheus-prefect-exporter
          helm:
            releaseName: prometheus-prefect-exporter
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-prometheus-prefect-exporter'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
