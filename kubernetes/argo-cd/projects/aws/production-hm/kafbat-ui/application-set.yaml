---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kafbat-ui
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/aws/production-hm/kafbat-ui/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-hm-kafbat-ui'
    spec:
      project: production-hm
      sources:
        - repoURL: https://kafbat.github.io/helm-charts
          targetRevision: '{{.helmChartVersion}}'
          chart: kafka-ui
          helm:
            releaseName: kafbat-ui
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-kafbat-ui'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
