---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-open-webui
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/aws/production-hm/open-webui/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-hm-open-webui'
    spec:
      project: production-hm
      sources:
        - repoURL: https://helm.openwebui.com
          targetRevision: '{{.helmChartVersion}}'
          chart: open-webui
          helm:
            releaseName: open-webui
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-open-webui'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
