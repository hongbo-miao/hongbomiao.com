---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-doris-operator
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/aws/production-hm/doris-operator/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-hm-doris-operator'
    spec:
      project: production-hm
      sources:
        - repoURL: https://charts.selectdb.com
          targetRevision: '{{.helmChartVersion}}'
          chart: doris-operator
          helm:
            releaseName: doris-operator
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
      destination:
        namespace: '{{.environment}}-hm-doris-operator'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
