---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-cert-manager
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/aws/production-hm/cert-manager/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-hm-cert-manager'
    spec:
      project: production-hm
      sources:
        - repoURL: https://charts.jetstack.io
          targetRevision: '{{.helmChartVersion}}'
          chart: cert-manager
          helm:
            releaseName: cert-manager
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-cert-manager'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
