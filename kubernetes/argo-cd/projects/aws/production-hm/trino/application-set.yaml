---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-trino
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/aws/production-hm/trino/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-hm-trino'
    spec:
      project: production-hm
      sources:
        - repoURL: https://trinodb.github.io/charts
          targetRevision: '{{.helmChartVersion}}'
          chart: trino
          helm:
            releaseName: trino
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-trino'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
