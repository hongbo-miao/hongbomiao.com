---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-qdrant
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/aws/production-hm/qdrant/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-hm-qdrant'
    spec:
      project: production-hm
      sources:
        - repoURL: https://qdrant.github.io/qdrant-helm
          targetRevision: '{{.helmChartVersion}}'
          chart: qdrant
          helm:
            releaseName: qdrant
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-qdrant'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
