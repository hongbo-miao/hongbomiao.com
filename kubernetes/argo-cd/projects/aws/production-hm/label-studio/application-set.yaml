---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: label-studio
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/aws/production-hm/label-studio/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-hm-label-studio'
    spec:
      project: production-hm
      sources:
        - repoURL: https://charts.heartex.com
          targetRevision: '{{.helmChartVersion}}'
          chart: label-studio
          helm:
            releaseName: label-studio
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-label-studio'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
