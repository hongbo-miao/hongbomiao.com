---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-goldilocks
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/aws/production-hm/goldilocks/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-hm-goldilocks'
    spec:
      project: production-hm
      sources:
        - repoURL: https://charts.fairwinds.com/stable
          targetRevision: '{{.helmChartVersion}}'
          chart: goldilocks
          helm:
            releaseName: goldilocks
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
      destination:
        namespace: '{{.environment}}-hm-goldilocks'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
