---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: ray-cluster-valkey
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/aws/production-hm/ray-cluster-valkey/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-hm-ray-cluster-valkey'
    spec:
      project: production-hm
      sources:
        - repoURL: registry-1.docker.io/bitnamicharts
          targetRevision: '{{.helmChartVersion}}'
          chart: valkey
          helm:
            releaseName: ray-cluster-valkey
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-ray-cluster-valkey'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
