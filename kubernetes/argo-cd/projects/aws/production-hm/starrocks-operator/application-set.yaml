---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-starrocks-operator
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/aws/production-hm/starrocks-operator/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-hm-starrocks-operator'
    spec:
      project: production-hm
      sources:
        - repoURL: https://starrocks.github.io/starrocks-kubernetes-operator
          targetRevision: '{{.helmChartVersion}}'
          chart: operator
          helm:
            releaseName: starrocks-operator
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
      destination:
        namespace: '{{.environment}}-hm-starrocks-operator'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
