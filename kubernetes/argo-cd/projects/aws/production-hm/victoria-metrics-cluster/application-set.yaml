---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: production-victoria-metrics-cluster
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/aws/production-hm/victoria-metrics-cluster/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-hm-victoria-metrics-cluster'
    spec:
      project: production-hm
      sources:
        - repoURL: https://victoriametrics.github.io/helm-charts
          targetRevision: '{{.helmChartVersion}}'
          chart: victoria-metrics-cluster
          helm:
            releaseName: victoria-metrics-cluster
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-victoria-metrics-cluster'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
