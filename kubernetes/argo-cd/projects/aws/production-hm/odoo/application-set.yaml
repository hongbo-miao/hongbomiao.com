---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: odoo
  namespace: production-hm-argo-cd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
        revision: main
        files:
          - path: kubernetes/argo-cd/projects/aws/production-hm/odoo/environments/*/config.yaml
  template:
    metadata:
      name: '{{.environment}}-hm-odoo'
    spec:
      project: production-hm
      sources:
        - repoURL: registry-1.docker.io/bitnamicharts
          targetRevision: '{{.helmChartVersion}}'
          chart: odoo
          helm:
            releaseName: odoo
            valueFiles:
              - '$config/{{.path.path}}/helm-chart/values.yaml'
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          ref: config
        - repoURL: git@github.com:hongbo-miao/hongbomiao.com.git
          targetRevision: main
          path: '{{.path.path}}/kubernetes-manifests'
      destination:
        namespace: '{{.environment}}-hm-odoo'
        server: https://kubernetes.default.svc
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        automated:
          prune: true
