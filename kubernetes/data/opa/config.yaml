# https://www.openpolicyagent.org/docs/latest/configuration/

services:
  hm_service:
    url: http://log-receiver-service.hm:8000

decision_logs:
  service: hm_service
  console: true
  reporting:
    min_delay_seconds: 1
    max_delay_seconds: 10
