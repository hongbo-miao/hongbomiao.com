# https://www.openpolicyagent.org/docs/latest/configuration/

services:
  log_receiver:
    url: http://log-receiver-service.hm:41797

decision_logs:
  service: log_receiver
  console: true
  reporting:
    min_delay_seconds: 1
    max_delay_seconds: 10
