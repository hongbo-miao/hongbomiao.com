apiVersion: v1
kind: Service
metadata:
  name: opa-database-service
  namespace: hm
spec:
  type: ClusterIP
  selector:
    component: opa-database
  ports:
    - name: postgres
      protocol: TCP
      targetPort: postgres
      port: 40072
