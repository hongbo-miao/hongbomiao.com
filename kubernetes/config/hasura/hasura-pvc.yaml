apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hasura-pvc
  namespace: hm-hasura
  labels:
    app.kubernetes.io/name: hasura
spec:
  volumeName: hasura-pv
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 10Mi
