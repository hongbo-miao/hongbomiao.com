apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opa-pvc
  namespace: hm
spec:
  volumeName: opa-pv
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 100Mi
