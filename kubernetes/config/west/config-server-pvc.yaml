apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-server-pvc
  namespace: hm
spec:
  volumeName: config-server-pv
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 10Mi
