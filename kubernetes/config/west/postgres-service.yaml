apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: hm-postgres
spec:
  type: ClusterIP
  selector:
    component: opa-db
  ports:
    - name: postgres
      protocol: TCP
      targetPort: postgres
      port: 40072
