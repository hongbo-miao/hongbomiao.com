apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elastic-apm-pvc
spec:
  volumeName: elastic-apm-pv
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 100Mi
