apiVersion: v1
kind: ConfigMap
metadata:
  name: opa-db-configmap
  namespace: hm-opa
data:
  pgdata: "/var/lib/postgresql/data/pgdata"
  postgres_db: "postgres"
  postgres_user: "admin"
  postgres_password: "passw0rd"
