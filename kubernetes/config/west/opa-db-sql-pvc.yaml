apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opa-db-sql-pvc
  namespace: hm-opa
spec:
  volumeName: opa-db-sql-pv
  accessModes:
    - ReadWriteOnce
  storageClassName: opa-db-sql-storageclass
  resources:
    requests:
      storage: 10Mi
