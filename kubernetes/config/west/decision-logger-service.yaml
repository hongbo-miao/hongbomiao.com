apiVersion: v1
kind: Service
metadata:
  name: decision-logger-service
  namespace: hm
spec:
  type: ClusterIP
  selector:
    app: decision-logger
  ports:
    - name: decision-logger
      protocol: TCP
      targetPort: decision-logger
      port: 41797
