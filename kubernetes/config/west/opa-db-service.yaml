apiVersion: v1
kind: Service
metadata:
  name: opa-db-service
  namespace: hm-opa
spec:
  type: ClusterIP
  selector:
    component: opa-db
  ports:
    - name: postgres
      protocol: TCP
      targetPort: postgres
      port: 40072
