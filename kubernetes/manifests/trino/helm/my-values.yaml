# https://github.com/trinodb/charts/blob/main/charts/trino/values.yaml

---
service:
  port: 26869

# https://trino.io/docs/current/connector.html
additionalCatalogs:
  opa_db: |
    connector.name=postgresql
    connection-url=jdbc:postgresql://postgres-service.hm-postgres.svc:5432/opa_db
    connection-user=admin
    connection-password=passw0rd
  twitter_db: |
    connector.name=postgresql
    connection-url=jdbc:postgresql://timescale.hm-timescale.svc:5432/twitter_db
    connection-user=admin
    connection-password=passw0rd
