# https://github.com/bitnami/charts/blob/main/bitnami/spark/values.yaml

---
image:
  registry: docker.io
  repository: bitnami/spark
  tag: 3.3.2-debian-11-r29
master:
  configOptions:
    -Dspark.ui.reverseProxy=true
    -Dspark.ui.reverseProxyUrl=https://spark.hongbomiao.com
worker:
  configOptions:
    -Dspark.ui.reverseProxy=true
    -Dspark.ui.reverseProxyUrl=https://spark.hongbomiao.com
