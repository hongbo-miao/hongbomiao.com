apiVersion: v1
kind: Service
metadata:
  name: config-server-service
  namespace: hm
  labels:
    app.kubernetes.io/name: config-server
spec:
  type: ClusterIP
  selector:
    app: config-server
  ports:
    - name: config-server
      protocol: TCP
      targetPort: config-server
      port: 26660
