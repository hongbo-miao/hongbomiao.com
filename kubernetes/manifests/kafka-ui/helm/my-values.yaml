# https://github.com/provectus/kafka-ui/blob/master/charts/kafka-ui/values.yaml

---
yamlApplicationConfig:
  kafka:
    clusters:
      - name: yaml
        bootstrapServers: hm-kafka-kafka-bootstrap.hm-kafka.svc:9092
