# https://k3d.io/usage/configfile

apiVersion: k3d.io/v1alpha2
kind: Simple
kubeAPI:
  host: "k8s-hongbomiao.com"
  hostIP: "127.0.0.1"
  hostPort: "6441"
network: hm-network
ports:
  - port: 40001:80
    nodeFilters:
      - loadbalancer
options:
  k3s:
    extraServerArgs:
      - --no-deploy=traefik
      - --cluster-domain=east.k8s-hongbomiao.com
