const SUBSCRIBE_PING = '[Health] Subscribe Ping';
const RECEIVE_PING_SUCCEED = '[Health] Receive Ping Succeed';
const RECEIVE_PING_FAILED = '[Health] Receive Ping Failed';

const HealthActionType = {
  SUBSCRIBE_PING,
  RECEIVE_PING_SUCCEED,
  RECEIVE_PING_FAILED,
};

export default HealthActionType;
