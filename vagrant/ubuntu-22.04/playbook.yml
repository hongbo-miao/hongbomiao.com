---
- name: Install packages
  hosts: all
  gather_facts: false
  tasks:
    - name: Install packages from apt
      become: true
      become_user: root
      ansible.builtin.apt:
        name:
          - vnstat
        update_cache: true
        state: present
