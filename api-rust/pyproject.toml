[project]
name = "api-rust-scripts"
version = "1.0.0"
requires-python = "~=3.12.0"

[dependency-groups]
dev = [
  "httpx==0.28.1",
  "poethepoet==0.32.1",
  "torch==2.5.1",
  "torchvision==0.20.1",
]

[tool.uv]
package = false

[tool.poe.tasks]
download-model = "python scripts/download_model.py"
