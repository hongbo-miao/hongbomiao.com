[project]
name = "api-rust-scripts"
version = "1.0.0"

[dependency-groups]
dev = [
  "httpx==0.28.1",
  "onnx==1.19.0",
  "poethepoet==0.37.0",
  "torch==2.8.0",
  "torchvision==0.23.0",
]

[tool.uv]
package = false
required-version = ">=0.8.0"

[tool.poe.tasks]
download-model = "python scripts/download_model.py"
