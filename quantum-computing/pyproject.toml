[tool.poetry]
name = "quantum-computing"
version = "1.0.0"
description = ""
authors = ["Hongbo Miao"]

[tool.poetry.dependencies]
python = "3.11.x"
qiskit = {version = "0.42.1", extras = ["visualization"]}

[tool.poetry.group.dev.dependencies]
poethepoet = "0.19.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poe.tasks]
dev = "python main.py"
