[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "3.11.x"
qiskit = {version = "1.1.2", extras = ["visualization"]}

[tool.poetry.group.dev.dependencies]
poethepoet = "0.27.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poe.tasks]
dev = "python main.py"
